// first attempt to write insertion sort
#include <stdio.h>

void insertion_sort(int * source_arr, int * dest_arr, int size);

int main(void)
{
  // TODO: prepare input data and the algorithm core

  return 0;
}

void insertion_sort(int * source_arr, int * dest_arr, int size)
{

}

